import { useEffect } from 'react';

export default function Analytics() {
  useEffect(() => {
    // Placeholder: integrate Plausible or Google Analytics here.
    // Example Plausible snippet could be added in index.html or dynamically here.
    // window.plausible?.('pageview');
  }, []);
  return null;
}
